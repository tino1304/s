---
description: Add a new backlog item
---

**Args:** $ARGUMENTS

1. Read `.claude/s/backlog.md`. If missing → create it with the table header.
2. Use `$ARGUMENTS` as the TODO description.
3. Append a new row to the table with the next `#`, File as `—`, Line as `—`, and the TODO text.
4. Confirm the item was added.
